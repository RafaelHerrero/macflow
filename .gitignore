# SwiftPM build
.build/
.swiftpm/
*.xcodeproj

# macOS
.DS_Store

# LaunchAgent logs
*.log

# Personal config: only the template (config.toml.example) is versioned.
# install.sh generates config.toml from it on first run, so your personal
# shortcuts stay out of the repo. To version your own shortcuts in a fork,
# force-add it: `git add -f config.toml`.
config.toml
