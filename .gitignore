# SwiftPM build
.build/
.swiftpm/
*.xcodeproj

# macOS
.DS_Store

# LaunchAgent logs
*.log

# Personal config: the template (config.toml.example) is versioned;
# the real config.toml is yours — uncomment if you do NOT want to version it.
# config.toml
